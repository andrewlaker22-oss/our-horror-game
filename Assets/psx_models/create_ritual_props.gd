@tool
extends SceneTree

func _init():
    print("Generating basic PSX models like tombstones, candles, altars...")
    # This script will map these directly in Godot.
    var mesh = BoxMesh.new()
    print("Done")
    quit()
